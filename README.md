# Portfolio Site

## Development

- Install Hugo: `sudo apt install hugo` or `brew install hugo`
- Run dev server: `hugo server`

## Build

- Run `hugo -b $CF_PAGES_URL`

## Deploy

- Cloudflare pages deploy will trigger from `git push` to `main` branch.
