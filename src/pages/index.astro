---
import { GitHubIcon, LinkedInIcon, MailIcon } from '@components/icons';
import Base from '@layouts/base.astro';

const pageTitle = 'Ross Brandon — Software Engineer and Architect';
const description = 'Ross Brandon — Software Engineer and Architect.';

const personJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Person',
  name: 'Ross Brandon',
  url: 'https://rossbrandon.dev',
  jobTitle: 'Software Architect',
  worksFor: { '@type': 'Organization', name: 'Adobe' },
  sameAs: [
    'https://github.com/rossbrandon',
    'https://www.linkedin.com/in/ross-brandon/',
  ],
};
---

<Base pageTitle={pageTitle} description={description}>
  <script
    slot="head"
    type="application/ld+json"
    set:html={JSON.stringify(personJsonLd)}
  />
  <main>
    <h1>Ross Brandon</h1>
    <p class="subtitle">Software Engineer // Architect</p>

    <div class="social-icons">
      <a
        href="https://github.com/rossbrandon"
        target="_blank"
        rel="me noopener"
        title="GitHub"
        aria-label="GitHub"
      >
        <GitHubIcon />
      </a>

      <a
        href="https://www.linkedin.com/in/ross-brandon/"
        target="_blank"
        rel="me noopener"
        title="LinkedIn"
        aria-label="LinkedIn"
      >
        <LinkedInIcon />
      </a>

      <a
        href="mailto:contact-me.psmxi@simplelogin.com"
        target="_blank"
        rel="me noopener"
        title="Email"
        aria-label="Email"
      >
        <MailIcon />
      </a>
    </div>
  </main>
</Base>

<style>
  main {
    flex: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: var(--space-5);

    @media (max-width: 684px) {
      padding: var(--space-5) var(--space-8);
    }
  }

  h1 {
    margin-bottom: var(--space-4);

    @media (max-width: 684px) {
      font-size: var(--text-2xl);
    }
  }

  .subtitle {
    color: var(--color-text-secondary);
  }

  .social-icons {
    display: flex;
    gap: var(--space-6);
    align-items: center;
    justify-content: center;
    margin-top: var(--space-6);
  }

  .social-icons a {
    display: inline-flex;
    color: var(--color-text-secondary);
    text-decoration: none;
    transition:
      transform var(--duration-fast) var(--ease-standard),
      color var(--duration-fast) var(--ease-standard);
  }

  .social-icons a:hover {
    transform: translateY(-2px);
    color: var(--accent);
  }

  .social-icons :global(svg) {
    width: 24px;
    height: 24px;
  }
</style>
